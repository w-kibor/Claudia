# Python dependencies for Claudia Data Pipeline
# Keep this lightweight and installable in CI.

# Used by sampling scripts.
pandas>=2.0.0
numpy>=1.24.0

# Utility libraries for future pipeline extensions.
python-dotenv>=1.0.0
python-dateutil>=2.8.0
